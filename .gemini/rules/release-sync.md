# Antigravity Rule: release-sync
"Use when detecting release semver version drift across VERSION, GEMINI.md, and package.json, maintaining CHANGELOG and README release hygiene, or atomically bumping release versions"

Trigger: Invoke when working on tasks matching: "Use when detecting release semver version drift across VERSION, GEMINI.md, and package.json, maintaining CHANGELOG and README release hygiene, or atomically bumping release versions"
Canonical Skill Reference: `.agents/skills/release-sync/SKILL.md`
